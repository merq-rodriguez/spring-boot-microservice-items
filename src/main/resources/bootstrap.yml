server:
  port: 3000

spring:
  application:
    name: items-service
  #profiles:
  #  active: dev
    
  cloud:
    config:
      uri: http://localhost:8888
      label: master


management:
  endpoints:
    web:
      exposure:
        include: "*"